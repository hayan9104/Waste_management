node_modules/
dist/
build/
.vite/

# env — never commit real secrets, only the .example templates
.env
.env.local
.env.*.local
!.env.example

# local db data
.data/

# uploaded complaint photos / resolution proof — regenerated by `npm run seed`
# (the API creates this directory on demand, so nothing needs to be tracked here)
backend/api/uploads/*

# python (backend/vision — FastAPI/YOLOv8 service)
backend/vision/__pycache__/
backend/vision/**/__pycache__/
*.pyc
.venv/
venv/

# logs
*.log
npm-debug.log*

# editor / os
.vscode/
.idea/
.DS_Store
Thumbs.db
