# API origin used by the built app. In dev, Vite proxies /api so this can stay empty.
VITE_API_URL=
# Optional: override the OSM tile URL (e.g. a self-hosted style)
VITE_TILE_URL=
# Optional: override the aerial/satellite tile URL (maps default to satellite)
VITE_SATELLITE_TILE_URL=
